---
name: php
description: "Php for newecome. 7 gotchas, 7 conventions, 7 fixes."
domain: php
triggers:
  - glob: "**/*.php"
autoGenerated: true
enabled: true
---

# Php

Auto-compiled from **78 real patterns** in **newecome**. This skill is auto-routed to agents when working on php files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| ⚠️ GOTCHA: Updated your database schema — prevents | -         $enabled = ($config['blog_auto_post'] ?? false) == true; +         // Hardcoded to true fo |
| ⚠️ GOTCHA: Replaced auth Route | -         Route::get('discord/commands',         [DiscordController::class, 'commands']); +          |
| ⚠️ GOTCHA: Replaced auth Route — ensures atomic mu | -     Route::post('products/bulk',              [ProductController::class, 'bulkAction'])->middlewar |
| ⚠️ GOTCHA: Added OAuth2 authentication — prevents  | -         $redirectUri = rtrim(env('APP_URL', 'https://upgradercx.com'), '/') . '/api/admin/pinteres |
| ⚠️ GOTCHA: Replaced auth Telegram | -                 Log::channel('automation')->warning("Telegram: sendPhoto failed. Response: " . $re |
| ⚠️ GOTCHA: Fixed null crash in Simple — prevents n | -             // Simple formatting for Telegram +             // Simple formatting for Telegram - St |
| ⚠️ GOTCHA: Fixed null crash in Telegram | -                 // Ensure photo URL is absolute for Telegram +                 $filename = basenam |

## 🔧 Problem Playbooks

### Fixed null crash in Models — prevents null/undefined runtime crashes
- use App\Services\Suppliers\SupplierServiceFactory;
+ use App\Models\Category;
- use Illuminate\Console\Command;
+ use App\Services\Suppliers\SupplierServiceFactory;
- use Exception;
+ use Illuminate\Console\Command;
- 
+ use Illuminate\Support\Str;
- class SyncSupplierProducts extends Command
+ use Exception;
- {
+ 
-     /**
+ class SyncSupplierProducts extends Command
-      * The name and sig

**Actionable Steps:**
1. Modified 1 files
2. identifier: App
3. identifier: Models
4. identifier: Category
5. identifier: Services

### Fixed null crash in Fetch — reduces initial bundle size with code splitting
-      * Fetch latest products from a supplier.
+      * Fetch latest products from a supplier (Async via Job).
-     public function fetchProducts(int $id): JsonResponse
+     public function fetchProducts(int $id, Request $request): JsonResponse
-         SupplierConnection::findOrFail($id);
+         $supplier = SupplierConnection::findOrFail($id);
-         Artisan::call('app:sync-supplier-pro

**Actionable Steps:**
1. Modified 1 files
2. identifier: Fetch
3. identifier: Async
4. identifier: Job
5. identifier: Request

### problem-fix in SyncSupplierProducts.php
File updated (external): api/app/Console/Commands/SyncSupplierProducts.php

Content summary (152 lines):
<?php

namespace App\Console\Commands;

use App\Models\SupplierConnection;
use App\Models\SupplierProduct;
use App\Models\SupplierSyncLog;
use App\Services\Suppliers\SupplierServiceFactory;
use Illuminate\Console\Command;
use Exception;

class SyncSupplierProducts extends Command
{
    /**
    

**Actionable Steps:**
1. Modified 1 files

### Fixed null crash in Telegram
-                 if (!str_starts_with($photoUrl, 'http')) {
+                 Log::channel('automation')->info("Telegram debug post {$post->id}: image_url from DB = {$photoUrl}");
-                     $photoUrl = rtrim($websiteUrl, '/') . '/' . ltrim($photoUrl, '/');
+ 
-                 }
+                 if (!str_starts_with($photoUrl, 'http')) {
- 
+                     $photoUrl = rtrim($we

**Actionable Steps:**
1. Modified 1 files
2. identifier: Log
3. identifier: Telegram
4. identifier: Final
5. identifier: HTML

### Fixed null crash in Telegram
-             // If we have an image, use sendPhoto. Otherwise sendSendMessage.
+             // Telegram sendPhoto caption limit is 1024 characters.
-             if ($post->image_url) {
+             if (strlen($caption) > 1000) {
-                 // Ensure photo URL is absolute for Telegram
+                 $caption = Str::limit($caption, 990) . "\n\n🔗 <a href='{$postUrl}'>Read Full Article<

**Actionable Steps:**
1. Modified 1 files
2. identifier: Telegram
3. identifier: Str
4. identifier: Read
5. identifier: Full

### problem-fix in AdminBlogAutomationController.php
-     }
+         public function sendPostToTelegram(int $id): JsonResponse
- }
+     {
- 
+         $post = \App\Models\BlogPost::findOrFail($id);
+         $service = new TelegramService();
+         $success = $service->sendBlogPost($post);
+ 
+         if ($success) {
+             return response()->json(['message' => 'Blog post sent to Telegram!']);
+         }
+ 
+         return response()

**Actionable Steps:**
1. Modified 1 files

### problem-fix in AdminBlogAutomationController.php
-         return response()->json([
+         $error = $result['description'] ?? 'Unknown Error (Check bot permissions)';
-             'message' => 'Telegram Test Failed: ' . ($result['description'] ?? 'Unknown Error'),
+         Log::channel('automation')->error("Telegram Test Failed: " . json_encode($result));
-         ], 400);
+ 
-     }
+         return response()->json([
- }
+             '

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **what-changed in ProductObserver.php — confirmed 3x** — -         // Check if individual product discord is enabled
+         Log::channel('automation')->in
- 📐 **convention in AutomationController.php** — File updated (external): api/app/Http/Controllers/AutomationController.php

Content summary (217 lin
- 📐 **Updated API endpoint Route — confirmed 3x** — -             Route::get('keywords',             [AdminBlogKeywordController::class, 'index']);
+   
- 📐 **what-changed in PinterestService.php — confirmed 3x** — -         $redirectUri = rtrim(env('APP_URL', 'https://upgradercx.com'), '/') . '/api/admin/pinteres
- 📐 **Added OAuth2 authentication — prevents null/undefined runtime crashes — confirmed 3x** — -         Log::info("Pinterest: Attempting token exchange for Client ID: " . substr($clientId, 0, 4)
- 📐 **what-changed in PinterestService.php — confirmed 3x** — -         $redirectUri = rtrim(config('app.url'), '/') . '/api/admin/pinterest/callback';
+         
- 📐 **problem-fix in AdminBlogAutomationController.php — confirmed 3x** — -         public function sendPostToTelegram(int $id): JsonResponse
+     }
-     {
+ 
-         $po

---
*Auto-generated by BrainSync 🧠 | 78 patterns | 2026-03-22*
