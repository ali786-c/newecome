---
name: php
description: "Php for newecome. 3 gotchas, 2 conventions, 4 fixes."
domain: php
triggers:
  - glob: "**/*.php"
autoGenerated: true
enabled: true
---

# Php

Auto-compiled from **34 real patterns** in **newecome**. This skill is auto-routed to agents when working on php files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| ⚠️ GOTCHA: Replaced auth Telegram | -                 Log::channel('automation')->warning("Telegram: sendPhoto failed. Response: " . $re |
| ⚠️ GOTCHA: Fixed null crash in Simple — prevents n | -             // Simple formatting for Telegram +             // Simple formatting for Telegram - St |
| ⚠️ GOTCHA: Fixed null crash in Telegram | -                 // Ensure photo URL is absolute for Telegram +                 $filename = basenam |

## 🔧 Problem Playbooks

### Fixed null crash in Telegram
-                 if (!str_starts_with($photoUrl, 'http')) {
+                 Log::channel('automation')->info("Telegram debug post {$post->id}: image_url from DB = {$photoUrl}");
-                     $photoUrl = rtrim($websiteUrl, '/') . '/' . ltrim($photoUrl, '/');
+ 
-                 }
+                 if (!str_starts_with($photoUrl, 'http')) {
- 
+                     $photoUrl = rtrim($we

**Actionable Steps:**
1. Modified 1 files
2. identifier: Log
3. identifier: Telegram
4. identifier: Final
5. identifier: HTML

### Fixed null crash in Telegram
-             // If we have an image, use sendPhoto. Otherwise sendSendMessage.
+             // Telegram sendPhoto caption limit is 1024 characters.
-             if ($post->image_url) {
+             if (strlen($caption) > 1000) {
-                 // Ensure photo URL is absolute for Telegram
+                 $caption = Str::limit($caption, 990) . "\n\n🔗 <a href='{$postUrl}'>Read Full Article<

**Actionable Steps:**
1. Modified 1 files
2. identifier: Telegram
3. identifier: Str
4. identifier: Read
5. identifier: Full

### problem-fix in AdminBlogAutomationController.php
-     }
+         public function sendPostToTelegram(int $id): JsonResponse
- }
+     {
- 
+         $post = \App\Models\BlogPost::findOrFail($id);
+         $service = new TelegramService();
+         $success = $service->sendBlogPost($post);
+ 
+         if ($success) {
+             return response()->json(['message' => 'Blog post sent to Telegram!']);
+         }
+ 
+         return response()

**Actionable Steps:**
1. Modified 1 files

### problem-fix in AdminBlogAutomationController.php
-         return response()->json([
+         $error = $result['description'] ?? 'Unknown Error (Check bot permissions)';
-             'message' => 'Telegram Test Failed: ' . ($result['description'] ?? 'Unknown Error'),
+         Log::channel('automation')->error("Telegram Test Failed: " . json_encode($result));
-         ], 400);
+ 
-     }
+         return response()->json([
- }
+             '

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **what-changed in PinterestService.php — confirmed 3x** — -         $redirectUri = rtrim(config('app.url'), '/') . '/api/admin/pinterest/callback';
+         
- 📐 **problem-fix in AdminBlogAutomationController.php — confirmed 3x** — -         public function sendPostToTelegram(int $id): JsonResponse
+     }
-     {
+ 
-         $po

---
*Auto-generated by BrainSync 🧠 | 34 patterns | 2026-03-21*
