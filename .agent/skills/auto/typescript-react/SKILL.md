---
name: typescript-react
description: "Typescript React for newecome. 6 gotchas, 9 conventions, 3 fixes."
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **48 real patterns** in **newecome**. This skill is auto-routed to agents when working on typescript-react files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| ⚠️ GOTCHA: Replaced auth Progress — prevents null/ | -     /* ── Discord Queries ── */ +  -     const { data: discordRes, refetch: refetchDiscord } = u |
| ⚠️ GOTCHA: Replaced auth Progress — prevents null/ | - import { Progress } from '@/components/ui/progress'; + import { useNavigate } from 'react-router- |
| ⚠️ GOTCHA: Updated for database schema — prevents  | -   FileText, Clock, AlertTriangle, XCircle, CalendarIcon, Layout +   FileText, Clock, AlertTriangle |
| ⚠️ GOTCHA: Replaced auth Discord — prevents null/u | -     /* ── Progress Polling ── */ +     /* ── Discord Queries ── */ -     useEffect(() => { +    |
| ⚠️ GOTCHA: Updated for database schema — prevents  | -   FileText, Clock, AlertTriangle, XCircle, CalendarIcon, +   FileText, Clock, AlertTriangle, XCirc |
| ⚠️ GOTCHA: Replaced auth Settings — prevents null/ | -     Settings, Zap, Bot, History, MessageSquare, Send, RefreshCw +     Settings, Zap, Bot, History |

## 🔧 Problem Playbooks

### Fixed null crash in Time — prevents null/undefined runtime crashes
-   const highCount = tickets.filter((t) => (t.priority === 'high' || t.priority === 'urgent') && t.status !== 'closed').length;
+   const waitingCount = tickets.filter((t) => t.status === 'waiting_customer').length;
-   const canReply = selectedTicket && selectedTicket.status !== 'closed';
+   const highCount = tickets.filter((t) => (t.priority === 'high' || t.priority === 'urgent') && t.status !

**Actionable Steps:**
1. Modified 1 files
2. identifier: Time
3. identifier: Date
4. identifier: Math
5. identifier: Support

### problem-fix in Tickets.tsx
-   const canReply = selectedTicket && ['open', 'pending', 'answered', 'waiting_customer'].includes(selectedTicket.status);
+   const canReply = selectedTicket && !['closed', 'resolved'].includes(selectedTicket.status);

📌 IDE AST Context: Modified symbols likely include [statusConfig, priorityConfig, categoryLabels, Tickets]

**Actionable Steps:**
1. Modified 1 files

### problem-fix in Tickets.tsx
-   const canReply = selectedTicket && ['open', 'pending', 'answered'].includes(selectedTicket.status);
+   const canReply = selectedTicket && ['open', 'pending', 'answered', 'waiting_customer', 'resolved'].includes(selectedTicket.status);

📌 IDE AST Context: Modified symbols likely include [statusConfig, priorityConfig, categoryLabels, AdminTickets]

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Strengthened types SelectItem — prevents null/undefined runtime crashes** — -                 <SelectItem value="resolved">Resolved</SelectItem>
+                 <SelectItem v
- 📐 **problem-fix in Tickets.tsx — confirmed 3x** — -   const canReply = selectedTicket && ['open', 'pending', 'answered', 'waiting_customer', 'resolved
- 📐 **Strengthened types Waiting — prevents null/undefined runtime crashes** — -   resolved: { label: 'Resolved', variant: 'default' },
+   waiting_customer: { label: 'Waiting for
- 📐 **Fixed null crash in Authorization — prevents null/undefined runtime crashes — confirmed 3x** — -             await axios.post(`${API_BASE_URL}/admin/merchants/${merchant.id}/configs`, {
+        
- 📐 **what-changed in Tickets.tsx — confirmed 3x** — -                           <Badge variant="outline" className="text-[9px] px-1.5">{categoryLabels[t
- 📐 **Replaced auth PinterestTabContent — prevents null/undefined runtime crashes — confirmed 3x** — -                 </Tabs>
+                         <PinterestTabContent 
- 
+                   
- 📐 **Strengthened types Discord — reduces initial bundle size with code splitting** — -                   <div className="py-2 border-y space-y-3">
+                   <div className="py

## 🤔 Decisions & Trade-offs

- **decision in Tickets.tsx** — -   const canReply = selectedTicket && ['open', 'pending', 'answered'].includes(selectedTicket.statu

---
*Auto-generated by BrainSync 🧠 | 48 patterns | 2026-03-22*
