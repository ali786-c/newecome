---
name: typescript-react
description: "Typescript React for newecome. 2 gotchas, 1 conventions."
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **13 real patterns** in **newecome**. This skill is auto-routed to agents when working on typescript-react files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

### ❌ ⚠️ GOTCHA: Updated for database schema — prevents null/undefined runtime crashes
-   FileText, Clock, AlertTriangle, XCircle, CalendarIcon,
+   FileText, Clock, AlertTriangle, XCircle, CalendarIcon, Layout
-   const openEditor = (post?: BlogPost) => {
+   const sendToPinterestMutation = useApiMutation(
-     setEditingPost(post);
- Modified 1 files
- identifier: FileText
- identifier: Clock

### ❌ ⚠️ GOTCHA: Replaced auth Settings — prevents null/undefined runtime crashes
-     Settings, Zap, Bot, History, MessageSquare, Send, RefreshCw
+     Settings, Zap, Bot, History, MessageSquare, Send, RefreshCw, Layout
-     /* ── Progress Polling ── */
+     /* ── Pinterest Queries ── */
-     useEffect(() => {
+     cons
- Modified 1 files
- identifier: Settings
- identifier: Zap


## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Strengthened types BlogEditor — prevents null/undefined runtime crashes** — - import { BlogEditor } from '@/components/blog/BlogEditor';
+ import { automationApi } from '@/api/

---
*Auto-generated by BrainSync 🧠 | 13 patterns | 2026-03-21*
