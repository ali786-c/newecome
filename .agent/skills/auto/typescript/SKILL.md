---
name: typescript
description: "Typescript for newecome. 4 conventions."
domain: typescript
triggers:
  - glob: "**/*.ts"
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript

Auto-compiled from **11 real patterns** in **newecome**. This skill is auto-routed to agents when working on typescript files.

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Strengthened types Promise — reduces initial bundle size with code splitting** — -     if (USE_MOCK) return mockDelay({ data: MOCK_MODULES });
+     const res = await client.get('/a
- 📐 **convention in automation.api.ts** — - };
+   async getDiscordConfig(): Promise<ApiResponse<any>> {
- 
+     const res = await client.get
- 📐 **convention in automation.api.ts** — - };
+   async getPinterestConfig(): Promise<ApiResponse<any>> {
- 
+     const res = await client.g
- 📐 **convention in index.ts** — File updated (external): upgradercx-source updated/artifacts/upgradercx/src/types/index.ts

Content 

---
*Auto-generated by BrainSync 🧠 | 11 patterns | 2026-03-22*
