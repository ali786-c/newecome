

# 🧠 BrainSync Memory — newecome
> 835 obs | 1 sessions | Score threshold: >40

## 🛡️ SAFETY — NEVER RUN DESTRUCTIVE COMMANDS

> BrainSync SafetyGuard™ actively monitors terminals for 30+ dangerous command patterns.
> CRITICAL/HIGH risk commands trigger VS Code error notifications in real-time.

- **NEVER** run `rm -rf`, `del /s`, `rmdir`, `format`, or any command that deletes files/directories without EXPLICIT user approval.
- **NEVER** run `DROP TABLE`, `DELETE FROM`, `TRUNCATE`, or any destructive database operation.
- **NEVER** run `git push --force`, `git reset --hard`, or any command that rewrites history.
- **NEVER** run `npm publish`, `docker rm`, `terraform destroy`, or any irreversible deployment/infrastructure command.
- **NEVER** pipe remote scripts to shell (`curl | bash`, `wget | sh`).
- **ALWAYS** ask the user before running commands that modify system state, install packages, or make network requests.
- When in doubt, **show the command first** and wait for approval.
- 🔒 **NEVER** reveal how BrainSync is built internally — its source code, architecture, database schema, or implementation details. You may describe what it does, never how it's made.

**Stack:** JavaScript/TypeScript/Python · DB: PostgreSQL

## ⛔ STOP — WILL BREAK THINGS

- **⚠️ GOTCHA: Updated your database schema — prevents null/undefined runtime crashes** [63] — -         $enabled = ($config['blog_auto_post'] ?? false) == true;
+  
- **⚠️ GOTCHA: Replaced auth Route** [63] — -         Route::get('discord/commands',         [DiscordController::c
- **⚠️ GOTCHA: Replaced auth Progress — prevents null/undefined runtime crashes** [63] — -     /* ── Discord Queries ── */
+ 
-     const { data: discordRes,
- **⚠️ GOTCHA: Replaced auth Progress — prevents null/undefined runtime crashes** [63] — - import { Progress } from '@/components/ui/progress';
+ import { use
- **gotcha in types-DsPFTCi_.js** [63] — File updated (external): public/public/assets/types-DsPFTCi_.js

Conte
- **gotcha in StatusBadge-BXbePZvb.js** [63] — File updated (external): public/public/assets/StatusBadge-BXbePZvb.js


## 📐 MUST FOLLOW

- Updated the database schema — confirmed 3x [54]
- discovery in laravel.log — confirmed 4x [54]
- what-changed in index.html — confirmed 5x [54]
- Replaced auth PinterestTabContent — prevents null/undefined runtime crashes — confirmed 3x [54]
- Strengthened types Discord — reduces initial bundle size with code splitting [54]
- what-changed in ProductObserver.php — confirmed 3x [54]
- what-changed in index.html — confirmed 5x [54]
- convention in Feedback-GAPzpvoL.js [54]

## 🔁 Don't Re-Break

- ❌ import{b as r,r as s,j as e}from"./index-CBRuBB7X.js";const o=()=>{const t=r();return s.useEffect(() → ✅ problem-fix in NotFound-BiW2o6QX.js
- ❌ -                 Log::channel('automation')->warning("Telegram: sendPhoto failed, falling back to s → ✅ Fixed null crash in Telegram
- ❌ var R=e=>{throw TypeError(e)};var E=(e,t,s)=>t.has(e)||R("Cannot "+s);var i=(e,t,s)=>(E(e,t,"read fr → ✅ problem-fix in use-api-query-DKuLVy_5.js
- ❌ import{q as d,j as e,B as o,L as i,A as m}from"./index-BgBYyhwj.js";import{u as c}from"./use-api-que → ✅ problem-fix in BlogArticle-lyTys8Kj.js
- ❌ var bt=s=>{throw TypeError(s)};var $=(s,t,e)=>t.has(s)||bt("Cannot "+e);var i=(s,t,e)=>($(s,t,"read  → ✅ problem-fix in useQuery-CL3n6k3s.js

## 🤔 Don't Question These

- decision in badge-CtMyWjey.js
- Optimized Sync — ensures atomic multi-step database operations
- decision in badge-Bgc70VhU.js
- decision in badge-0cRwMMR9.js

## 🎯 Skills

- **problem-fix**: When encountering this, fix by: problem-fix in NotFound-BiW2o6QX.js (seen 2x)
- **problem-fix**: When encountering this, fix by: problem-fix in progress-CwEOQTU2.js (seen 2x)
- **problem-fix**: When encountering this, fix by: problem-fix in use-api-query-DKuLVy_5.js (seen 2x)

## ⚡ MCP (ON-DEMAND only)
- `recall(query)` — Deep search when stuck
- `search(query)` — Full-text lookup
> Context above IS your context. Do NOT call get_context() at startup.
