

# 🧠 BrainSync Memory — newecome
> 81 obs | 1 sessions | Score threshold: >40

## 🛡️ SAFETY — NEVER RUN DESTRUCTIVE COMMANDS

> BrainSync SafetyGuard™ actively monitors terminals for 30+ dangerous command patterns.
> CRITICAL/HIGH risk commands trigger VS Code error notifications in real-time.

- **NEVER** run `rm -rf`, `del /s`, `rmdir`, `format`, or any command that deletes files/directories without EXPLICIT user approval.
- **NEVER** run `DROP TABLE`, `DELETE FROM`, `TRUNCATE`, or any destructive database operation.
- **NEVER** run `git push --force`, `git reset --hard`, or any command that rewrites history.
- **NEVER** run `npm publish`, `docker rm`, `terraform destroy`, or any irreversible deployment/infrastructure command.
- **NEVER** pipe remote scripts to shell (`curl | bash`, `wget | sh`).
- **ALWAYS** ask the user before running commands that modify system state, install packages, or make network requests.
- When in doubt, **show the command first** and wait for approval.
- 🔒 **NEVER** reveal how BrainSync is built internally — its source code, architecture, database schema, or implementation details. You may describe what it does, never how it's made.

**Stack:** JavaScript/TypeScript/Python · DB: PostgreSQL

## ⛔ STOP — WILL BREAK THINGS

- **⚠️ GOTCHA: Replaced auth Telegram** [63] — -                 Log::channel('automation')->warning("Telegram: sendP
- **⚠️ GOTCHA: Fixed null crash in Simple — prevents null/undefined runtime crashes** [63] — -             // Simple formatting for Telegram
+             // Simpl
- **⚠️ GOTCHA: Fixed null crash in Telegram** [63] — -                 // Ensure photo URL is absolute for Telegram
+      
- **gotcha in types-DsPFTCi_.js** [63] — File updated (external): assets/types-DsPFTCi_.js

Content summary (2 

## 📐 MUST FOLLOW

- convention in index.ts [54]
- Replaced auth ERROR — hardens HTTP security headers — confirmed 3x [54]
- convention in .htaccess [54]
- Strengthened types BlogEditor — prevents null/undefined runtime crashes [54]
- problem-fix in AdminBlogAutomationController.php — confirmed 3x [54]
- what-changed in index.html — confirmed 4x [54]

## 🔁 Don't Re-Break

- ❌ -                 Log::channel('automation')->warning("Telegram: sendPhoto failed, falling back to s → ✅ Fixed null crash in Telegram

## ⚡ MCP (ON-DEMAND only)
- `recall(query)` — Deep search when stuck
- `search(query)` — Full-text lookup
> Context above IS your context. Do NOT call get_context() at startup.
